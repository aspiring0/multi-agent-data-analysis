# FastAPI 后端依赖
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6
pydantic==2.5.0
python-dotenv==1.0.0

# LangGraph 和 PostgreSQL
langgraph==0.2.0
langgraph-checkpoint-postgres==1.0.0
psycopg2-binary==2.9.9

# 现有项目依赖（从主 requirements.txt 复制）
# 这里只列出关键依赖，完整依赖见主 requirements.txt
